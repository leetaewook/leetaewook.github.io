title: My Blog Name
title_seperator: '-' # default is '-'
locale: ko-KR # default is none
date_format: '%Y. %m. %d' #default is "%B %-d, %Y"

siteinfo:
  name: NameNameNameNameNameName
  description: DescriptionDescriptionDescriptionDescriptionDescription
  avatar: /assets/images/avatar.png
  socials:
    # github: https://github.com/leetaewook

permalink: /:title

pagination:
  enabled: true
  per_page: 4
  permalink: '/page:num/'
  limit: 0
  sort_field: 'date'
  sort_reverse: true

autopages:
  enabled: true
  collections:
    enabled: false
  tags:
    enabled: true
    layouts:
      - autopage_tags.html

defaults:
  - values:
      layout: post

plugins:
  - jekyll-paginate-v2
